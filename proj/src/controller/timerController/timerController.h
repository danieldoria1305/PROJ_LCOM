#ifndef TIMER_CONTROLLER_H
#define TIMER_CONTROLLER_H

/**
 * @brief Sets the timer's minutes and seconds.
 */
void (setTimer)();

/**
 * @brief Handles the timer's interrupts.
 */
void (timerInterruptHandler)();

#endif
