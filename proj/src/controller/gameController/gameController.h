#ifndef GAME_CONTROLLER_H
#define GAME_CONTROLLER_H

/**
 * @brief Initializes the game by setting the game elements to their initial positions, loading the pixmaps and setting the timer.
 */
void (initializeGame)();

#endif
